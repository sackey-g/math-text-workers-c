## 数式画像文字起こし（KaTeX）指示書

### 1. 全体の構成と見出し

* **見出しの形式**: 各問題は必ず `## 1・1` や `## 1` のように、`## 問題番号` の形式でスタートする。
* **配点や注記**: 大問の配点（例：配点 50）などは問題文の冒頭または末尾に原文通り配置する。

### 2. 数式のフォーマット

* **分数の表記**: 
  * **原則（本文・ブロック数式）**: 文字が小さくなるのを防ぐため、原則として分数は **`\dfrac`** を使用する。
  * **例外（添え字・指数の中）**: 積分記号（$\int$）の上下端や指数などの添え字部分に分数が入る場合は、文字が不自然に巨大化するのを防ぐため、必ず **`\frac`**（またはスラッシュ表記 `\pi/2` など）を使用する。  
    例：`\displaystyle\int_0^{\frac{\pi}{2}}\sin^n\theta \,d\theta`、`\int_0^{\frac{\pi}{4}} f(t)\sin 2t\,dt`
* **シグマ、limの上下添え字**: インライン数式内でシグマやlimの上下に添え字を表示しつつ、記号が大きくなりすぎるのを防ぐには、`\displaystyle` ではなく `\limits` を使用する（例：`\sum\limits_{n=1}^{n}` や `\lim\limits_{n \to \infty}`）。
* **積分記号（インテグラル）**: 文中（インライン数式）で積分記号が小さくなるのを防ぐため、記号の前に `\displaystyle` を付ける（例：`\displaystyle\int_{0}^{1}|e^t-x|\,dt`）。
* **微小量（$dx, dt$など）**: 積分の末尾の微小量の前には、必ず薄い空白 `\,` を挿入する（例：`\displaystyle\int f(x)\,dx`、`\int f(t)\,dt`）。
* **不等号**: 日本の表記に合わせて、必ず `\leqq` および `\geqq` を使用する（`\le`, `\leq` 等は不可）。
* **順列・組合せ**: 左添字が崩れないよう `{}` を用いて `{}_n\mathrm{C}_r`、`{}_n\mathrm{P}_r` と記述する。
* **ブロック数式（中央揃え）の配置**: `$$` は数式と同じ行に書かず、必ず前後に改行を入れて独立させる。
  例：
  $$
  \dfrac{1}{2} \leqq \dfrac{a^2+b^2+c^2}{(c-a)^2} < \dfrac{2}{3}
  $$
* **場合分けの表記**: 複数行にわたる関数の定義等は、ブロック数式内で `\begin{cases} ... \end{cases}` を使用する。
* **数式内の空白**: `$ $` 内でのカンマ区切りには、カンマの後に空白記号 `\ ` を挿入してスペースを空ける（例：`$x,\ y$`、`$a,\ b,\ c$`）。

### 3. テキストと改行の扱い

* **不自然な改行の禁止**: 原本のページ右端での折り返しによる、文中や単語の途中での改行は再現せず、1文として繋げて記述する。
* **意図的な改行（Markdown）**: 小問の区切りなど、意図的に改行を反映させる箇所では、行末に **半角スペース2個** を入れて改行する。
* **小問番号・カッコの扱い**:
  * (1), (2) や (i), (ii)、（ア）,（イ）など、原本の表記をそのまま忠実に再現する。
  * 日本語の文末につく条件のカッコ（例：`（$x>1$）` や `（$a$ は定数）`）は、日本語の組版に合わせて全角カッコ `（ ）` を使用する。数式の一部を囲む場合（例：$(x+1)^2$）のみ半角カッコ `( )` を用いる。