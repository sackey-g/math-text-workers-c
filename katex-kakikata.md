## 数式画像文字起こし（KaTeX）指示書

### 1. 全体の構成と見出し

- **見出しの形式**: 各問題は必ず `## 1・1` や `## 1` のように、`## 問題番号` の形式でスタートする。
- **配点や注記**: 大問の配点（例：配点 50）などは問題文の冒頭または末尾に原文どおり配置する。

### 2. 数式のフォーマット

- **分数の表記**: 文字が小さくなるのを防ぐため、分数は必ず `\dfrac` を使用する。
- **シグマ、limの上下添え字**: インライン数式では `\displaystyle` ではなく `\limits` を使用する（例：`\sum\limits_{k=1}^{n}`、`\lim\limits_{n \to \infty}`）。
- **積分記号**: インライン数式では積分記号の前に `\displaystyle` を付ける（例：`\displaystyle\int_{0}^{\beta}`）。
- **微小量**: 積分末尾の微小量の前に `\,` を挿入する（例：`\displaystyle\int f(x)\,dx`）。
- **不等号**: `\leqq` および `\geqq` を使用する。`\le`、`\leq`、`\ge`、`\geq` は使用しない。
- **順列・組合せ**: `{}_n\mathrm{C}_r`、`{}_n\mathrm{P}_r` と記述する。
- **ブロック数式**: 開始・終了の `$$` は数式と同じ行に書かず、必ず独立した行にする。

  ```text
  $$
  \dfrac{1}{2} \leqq \dfrac{a^2+b^2+c^2}{(c-a)^2} < \dfrac{2}{3}
  $$
  ```

- **場合分け**: 複数行にわたる関数定義などは、ブロック数式内で `\begin{cases} ... \end{cases}` を使用する。
- **数式内の空白**: カンマの後に `\ ` を挿入する（例：`$x,\ y$`）。

### 3. テキストと改行の扱い

- **不自然な改行の禁止**: 原本のページ右端での折り返しによる文中や単語途中の改行は再現せず、1文としてつなげる。
- **意図的な改行**: 小問の区切りなどでは、行末に半角スペース2個を入れる。
- **小問番号の統一**: `(1)`, `(2)`、`(i)`, `(ii)`、`（ア）`, `（イ）` など、原本の表記を忠実に再現する。

### 4. 自動整形と検証

```bash
npm run format:data
npm run validate:data
```

`format:data` は分数・不等号・インラインの `\sum` / `\lim` / `\int`・微小量・カンマ後の空白を機械的に整形する。`validate:data` はfrontmatter、3タブ用ファイル、見出し、数式記法を検証する。
