# Instructions for AI Coding Agents

この後期教材プロジェクト（`math-text-latter`）を保守・改修する AI エージェントは、以下のルールとアーキテクチャを厳守してください。

---

## 1. データ構造とビルドパイプラインの原則

1. **マスターデータは `data/` フォルダにある**:
   - `data/[subject]/[lecture]/` (`exercise.md`, `example.md`, `self_study.md`)
   - 教材を編集・追加する際は、**必ず `data/` フォルダ内の `.md` ファイルを直接編集** してください。
2. **`lib/subjectsData.js` は手動編集禁止**:
   - このファイルは `npm run build` / `npm run dev` 実行時に `scripts/generate_data.js` によって自動生成されます。
3. **静的エクスポート (`output: 'export'`) を維持すること**:
   - Cloudflare Workers の無料枠（CPU 10ms制限）を回避するため、全ページ完全 SSG（静的生成）構成になっています。
   - `next.config.mjs` の `output: 'export'` を解除したり、`fs` によるリクエスト時動的読み込みに戻さないでください。

---

## 2. 数式（KaTeX）および Markdown 記法ルール

- **見出し**: 各問題は必ず `## 1・1` または `## 1` のような `## 問題番号` で開始すること。配点や注記は原文どおり問題文の冒頭または末尾に置くこと。
- **分数**: インライン・ブロックを問わず `\dfrac` を使用すること。
- **シグマ・極限**: インライン数式の `\sum`・`\lim` には `\limits` を使用すること。添え字表示のために `\displaystyle` は使用しないこと。
- **積分**: インライン数式の積分記号には `\displaystyle` を付けること。微小量の前には `\,` を入れ、`\displaystyle\int f(x)\,dx` のように書くこと。
- **不等号**: `\leqq`・`\geqq` を使用し、`\le`・`\leq`・`\ge`・`\geq` は使用しないこと。
- **順列・組合せ**: `{}_n\mathrm{P}_r`・`{}_n\mathrm{C}_r` の形式で書くこと。
- **ブロック数式**: 開始・終了の `$$` は数式と同じ行に置かず、それぞれ独立した行にすること。
- **場合分け**: 複数行の関数定義などは、ブロック数式内で `\begin{cases} ... \end{cases}` を使用すること。
- **数式内の空白**: カンマ区切りには `$x,\ y$` のように `\ ` を挿入すること。
- **不自然な改行の禁止**: 原本のページ右端での折り返しは再現せず、文中・単語途中の改行をつなげること。
- **意図的な改行**: 小問の区切りなどには、行末に半角スペース2個を入れること。
- **小問番号**: `(1)`, `(2)`, `(i)`, `(ii)`, `（ア）`, `（イ）` など原本の表記を維持すること。
- **検証**: 教材編集後は `npm run validate:data` を実行すること。機械的に修正可能な記法は `npm run format:data` で整形できる。

---

## 3. Git 操作

ローカルでファイルを編集・追加・削除したら、必ずコミットしてください。リモート設定後は `git push` も行ってください（Cloudflare CI が自動ビルド・デプロイします）。
